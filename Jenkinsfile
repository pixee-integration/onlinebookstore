def cloneRepository() {
    echo "cloning repository"
    echo "checkout code"
    echo "checkout branch"
    echo "repo cloned successfully"
}

stages {
    stage("git_checkout") {
        steps {
            cloneRepository()
        }
    }
}
