pipeline {  
    agent any  
        stages {  
       	    stage("git_checkout") {  
           	    steps {  
              	    echo "cloning repository" 
                    echo "checkout code"
                    echo "checkout branch -1"
              	    echo "repo cloned successfully"  
              	    }  
         	    } 
        }
}
