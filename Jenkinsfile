pipeline {  
    agent any  
        stages {  
       	    stage("git_checkout") {  
           	    steps {  
              	    echo "cloning repository" 
                    echo "checkout code"
                    echo "checkout branch"
              	    echo "repo cloned successfully"  
              	    }  
         	    } 
        }
}
